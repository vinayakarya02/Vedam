# This repo uses split env files — do not put secrets here.
#
#   frontend/.env.local  → NEXT_PUBLIC_* (see frontend/.env.example)
#   backend/.env         → server secrets (see backend/.env.example)
